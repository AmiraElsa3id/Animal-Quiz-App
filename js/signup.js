import {validateSignupForm} from "validation.js";

const userName = document.querySelector(`input[placeholder="Pick a Username"]`);
const password = document.querySelector(`input[placeholder="Pick a Password"]`);
const form = document.querySelector(`form`);
const stdImage = querySelector("#std-img");
const stdPhone = querySelector("#phone");
const grade = querySelector("input[name='grade']");
const signupBtn = querySelector("button[type='submit']");

